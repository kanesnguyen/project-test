// import { importMasterData } from '../database/masters/masterImporter';

// const execute = async () => {
//   for (const tableName of ['m_question_levels']) {
//     await importMasterData(tableName);
//   }
//   process.kill(process.pid);
// };

// execute();
